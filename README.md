# pockerplan

Приложение для Planning Poker в реальном времени. Позволяет командам совместно оценивать задачи с помощью карточек, голосования и мгновенной синхронизации между участниками.

## Возможности

- Создание комнат с выбором шкалы оценки (Fibonacci, Power of 2, Linear, T-shirt)
- Присоединение к комнате по ссылке
- Голосование за задачи (тикеты) в реальном времени
- Скрытие голосов до момента раскрытия
- Администрирование комнаты: добавление тикетов, запуск/завершение голосования, пропуск тикетов
- Аватарки участников
- Переключение темы оформления: Системная / Тёмная / Светлая (с сохранением выбора)
- Быстрое голосование без тикетов: режим свободного голосования для быстрых оценок
- Горячие клавиши: ввод значения шкалы с клавиатуры, Enter (раскрыть), Space (сброс), стрелки (навигация по тикетам)
- Автоматическая очистка неактивных комнат
- Graceful shutdown сервера

## Стек технологий

- Backend: Go, Centrifuge (WebSocket), Kong (CLI), Zerolog (логирование)
- Frontend: React, TypeScript, Vite, React Router, Biome (линтер/форматтер)
- Коммуникация: WebSocket через Centrifuge
- Единый бинарник: фронтенд встраивается в Go-бинарник через `go:embed`

## Структура проекта

```
pockerplan/
  main.go              -- точка входа, запуск сервера
  ppback/
    hub/               -- Centrifuge-хаб, RPC-обработчики
    server/            -- HTTP-сервер, API-эндпоинты, SPA-фоллбэк
    room/              -- логика комнат и голосования
    model/             -- типы данных (Room, User, Ticket, Vote)
    scale/             -- шкалы оценки
    avatar/            -- аватарки
  ppfront/             -- React-приложение (Vite + TypeScript)
```

## Сборка и запуск

Требования: Go 1.25+, Node.js, npm.

Полная сборка (фронтенд + бэкенд):

```sh
make build
```

Собранный бинарник появится в `bin/pockerplan`.

Запуск:

```sh
./bin/pockerplan
```

По умолчанию сервер слушает на `:8080`. Адрес можно изменить флагом `--addr` или переменной окружения `ADDR`:

```sh
./bin/pockerplan --addr :3000
```

или

```sh
ADDR=:3000 ./bin/pockerplan
```

Справка по доступным флагам:

```sh
./bin/pockerplan --help
```

## Разработка

Запуск фронтенда (Vite dev server) и бэкенда одновременно:

```sh
make dev
```

Сборка только фронтенда:

```sh
make build-frontend
```

Сборка только бэкенда:

```sh
make build-backend
```

## Тесты

Запуск всех тестов:

```sh
make test
```

Только бэкенд:

```sh
make test-backend
```

Только фронтенд:

```sh
make test-frontend
```

## Линтинг и форматирование

Проверка кода (lint):

```sh
cd ppfront && npm run lint
```

Автоформатирование:

```sh
cd ppfront && npm run format
```

## API

| Эндпоинт                 | Метод | Описание                           |
|---------------------------|-------|------------------------------------|
| `/connection/websocket`   | WS    | WebSocket-подключение (Centrifuge) |
| `/api/scales`             | GET   | Список доступных шкал оценки      |
| `/api/avatars`            | GET   | Список аватарок                    |
| `/api/health`             | GET   | Проверка состояния сервера         |

## Протокол WebSocket

Коммуникация реализована через [Centrifuge](https://github.com/centrifugal/centrifuge). Все сообщения кодируются в JSON.

### Каналы

| Канал          | Направление       | Описание                     |
|----------------|-------------------|------------------------------|
| `room:{roomId}`| сервер → клиент   | Обновления состояния комнаты |

### RPC-методы (клиент → сервер)

Клиент вызывает методы через Centrifuge RPC. Каждый вызов получает JSON-ответ или ошибку.

#### Коды ошибок

| Код | Описание                                          |
|-----|---------------------------------------------------|
| 400 | Некорректный запрос или ошибка валидации          |
| 403 | Нет прав (неверный `adminSecret` или `userId`)    |
| 404 | Комната, тикет или пользователь не найдены        |
| 500 | Внутренняя ошибка сервера                         |

---

#### `create_room`

Создать новую комнату. Создатель автоматически становится администратором.

**Запрос:**
| Поле       | Тип    | Описание                        |
|------------|--------|---------------------------------|
| `scaleId`  | string | Идентификатор шкалы оценки      |
| `userName` | string | Отображаемое имя пользователя   |
| `avatarId` | string | Идентификатор аватарки          |

**Ответ:**
| Поле          | Тип    | Описание                              |
|---------------|--------|---------------------------------------|
| `roomId`      | string | Идентификатор созданной комнаты       |
| `adminSecret` | string | Секрет администратора (хранить локально) |
| `userId`      | string | Идентификатор пользователя            |
| `state`       | RoomSnapshot | Начальное состояние комнаты    |

---

#### `join_room`

Войти в существующую комнату. Вызывается после подписки на канал `room:{roomId}`.

**Запрос:**
| Поле       | Тип    | Описание                                              |
|------------|--------|-------------------------------------------------------|
| `roomId`   | string | Идентификатор комнаты                                 |
| `userName` | string | Отображаемое имя                                      |
| `avatarId` | string | Идентификатор аватарки                                |
| `userId`   | string | *(опц.)* Предыдущий ID пользователя при переподключении |

**Ответ:**
| Поле     | Тип          | Описание                              |
|----------|--------------|---------------------------------------|
| `userId` | string       | Идентификатор пользователя            |
| `state`  | RoomSnapshot | Текущее состояние комнаты             |

---

#### `submit_vote`

Проголосовать за текущий тикет.

**Запрос:**
| Поле     | Тип    | Описание                                      |
|----------|--------|-----------------------------------------------|
| `roomId` | string | Идентификатор комнаты                         |
| `userId` | string | Идентификатор пользователя                    |
| `value`  | string | Значение голоса (например, `"5"`, `"8"`, `"?"`) |

**Ответ:** `{}`

---

#### `remove_vote`

Отозвать свой голос.

**Запрос:**
| Поле     | Тип    | Описание                   |
|----------|--------|----------------------------|
| `roomId` | string | Идентификатор комнаты      |
| `userId` | string | Идентификатор пользователя |

**Ответ:** `{}`

---

#### `set_thinking`

Установить флаг «думает» для пользователя (используется heartbeat-механизмом).

**Запрос:**
| Поле       | Тип     | Описание                   |
|------------|---------|----------------------------|
| `roomId`   | string  | Идентификатор комнаты      |
| `userId`   | string  | Идентификатор пользователя |
| `thinking` | boolean | Состояние флага            |

**Ответ:** `{}`

---

#### `start_reveal` *(только администратор)*

Начать обратный отсчёт перед раскрытием голосов. Переводит комнату в состояние `counting_down`.

**Запрос:**
| Поле          | Тип    | Описание              |
|---------------|--------|-----------------------|
| `roomId`      | string | Идентификатор комнаты |
| `adminSecret` | string | Секрет администратора |

**Ответ:** `{}`

---

#### `reveal_votes` *(только администратор)*

Раскрыть голоса. Переводит комнату в состояние `revealed`.

**Запрос:**
| Поле          | Тип    | Описание              |
|---------------|--------|-----------------------|
| `roomId`      | string | Идентификатор комнаты |
| `adminSecret` | string | Секрет администратора |

**Ответ:** `{}`

---

#### `reset_votes` *(только администратор)*

Сбросить голоса и вернуться к голосованию.

**Запрос:**
| Поле          | Тип    | Описание              |
|---------------|--------|-----------------------|
| `roomId`      | string | Идентификатор комнаты |
| `adminSecret` | string | Секрет администратора |

**Ответ:** `{}`

---

#### `update_room_name` *(только администратор)*

Обновить название комнаты (макс. 200 символов).

**Запрос:**
| Поле          | Тип    | Описание              |
|---------------|--------|-----------------------|
| `roomId`      | string | Идентификатор комнаты |
| `adminSecret` | string | Секрет администратора |
| `name`        | string | Новое название        |

**Ответ:** `{}`

---

#### `add_ticket` *(только администратор)*

Добавить тикет в комнату (макс. 10 000 символов).

**Запрос:**
| Поле          | Тип    | Описание              |
|---------------|--------|-----------------------|
| `roomId`      | string | Идентификатор комнаты |
| `adminSecret` | string | Секрет администратора |
| `content`     | string | Описание тикета       |

**Ответ:**
| Поле       | Тип    | Описание                   |
|------------|--------|----------------------------|
| `ticketId` | string | Идентификатор нового тикета |

---

#### `set_ticket` *(только администратор)*

Установить активный тикет и запустить голосование.

**Запрос:**
| Поле          | Тип    | Описание                    |
|---------------|--------|-----------------------------|
| `roomId`      | string | Идентификатор комнаты       |
| `adminSecret` | string | Секрет администратора       |
| `ticketId`    | string | Идентификатор тикета        |

**Ответ:** `{}`

---

#### `next_ticket` / `prev_ticket` *(только администратор)*

Перейти к следующему или предыдущему тикету.

**Запрос:**
| Поле          | Тип    | Описание              |
|---------------|--------|-----------------------|
| `roomId`      | string | Идентификатор комнаты |
| `adminSecret` | string | Секрет администратора |

**Ответ:** `{}`

---

#### `start_free_vote` *(только администратор)*

Запустить быстрое голосование без тикета.

**Запрос:**
| Поле          | Тип    | Описание              |
|---------------|--------|-----------------------|
| `roomId`      | string | Идентификатор комнаты |
| `adminSecret` | string | Секрет администратора |

**Ответ:** `{}`

---

#### `interact_player`

Взаимодействие с другим игроком (например, бросок бумажки). Генерирует `RoomEvent` в ближайшем снапшоте.

**Запрос:**
| Поле           | Тип    | Описание                          |
|----------------|--------|-----------------------------------|
| `roomId`       | string | Идентификатор комнаты             |
| `userId`       | string | Инициатор взаимодействия          |
| `targetUserId` | string | Целевой пользователь              |
| `action`       | string | Тип действия (например, `"paper_throw"`) |

**Ответ:** `{}`

---

#### `theme_interact`

Взаимодействие с темой оформления (например, поджечь дерево у костра). Генерирует `RoomEvent` в ближайшем снапшоте.

**Запрос:**
| Поле     | Тип    | Описание                                       |
|----------|--------|------------------------------------------------|
| `roomId` | string | Идентификатор комнаты                          |
| `userId` | string | Идентификатор пользователя                     |
| `action` | string | Тип действия                                   |
| `data`   | object | Данные действия (зависит от `action`)          |

**Поддерживаемые действия:**

`action: "feed_fire"` — поджечь дерево (тема `campfire`):
| Поле     | Тип    | Описание                    |
|----------|--------|-----------------------------|
| `treeId` | number | Идентификатор дерева        |
| `fromX`  | number | X-координата броска         |
| `fromY`  | number | Y-координата броска         |

**Ответ:** `{}`

---

### Публикации (сервер → клиент)

Сервер публикует обновлённый `RoomSnapshot` в канал `room:{roomId}` при каждом изменении состояния комнаты.

#### RoomSnapshot

| Поле              | Тип              | Описание                                        |
|-------------------|------------------|-------------------------------------------------|
| `id`              | string           | Идентификатор комнаты                           |
| `name`            | string           | Название комнаты                                |
| `scale`           | string           | Идентификатор шкалы оценки                      |
| `state`           | RoomState        | Текущее состояние комнаты                       |
| `countdown`       | number           | Длительность обратного отсчёта (секунды)        |
| `users`           | User[]           | Список пользователей                            |
| `tickets`         | TicketSnapshot[] | Список тикетов                                  |
| `currentTicketId` | string           | ID активного тикета                             |
| `ticketsEnabled`  | boolean          | Включён ли режим тикетов                        |
| `events`          | RoomEvent[]      | *(опц.)* События этого обновления               |
| `themeState`      | ThemeState       | *(опц.)* Состояние темы оформления              |

**RoomState:**
```
"idle" | "voting" | "counting_down" | "revealed"
```

---

#### User

| Поле        | Тип     | Описание                         |
|-------------|---------|----------------------------------|
| `id`        | string  | Идентификатор пользователя       |
| `name`      | string  | Отображаемое имя                 |
| `avatarId`  | string  | Идентификатор аватарки           |
| `isAdmin`   | boolean | Является ли администратором      |
| `connected` | boolean | Активное подключение             |
| `thinking`  | boolean | Флаг «думает»                    |

---

#### TicketSnapshot

| Поле      | Тип          | Описание                                      |
|-----------|--------------|-----------------------------------------------|
| `id`      | string       | Идентификатор тикета                          |
| `content` | string       | Описание тикета                               |
| `status`  | TicketStatus | Состояние тикета                              |
| `votes`   | VoteInfo[]   | Голоса (значение скрыто до раскрытия)         |

**TicketStatus:**
```
"pending" | "voting" | "revealed" | "skipped"
```

**VoteInfo:**
| Поле     | Тип    | Описание                                               |
|----------|--------|--------------------------------------------------------|
| `userId` | string | Идентификатор пользователя                             |
| `value`  | string | Значение голоса (пусто пока `state != "revealed"`)     |

---

#### RoomEvent

Одноразовые события, включённые в снапшот и сброшенные после доставки.

| Поле      | Тип    | Описание                                                    |
|-----------|--------|-------------------------------------------------------------|
| `type`    | string | `"player_interaction"` или тип темы (`"theme_interaction"`) |
| `action`  | string | Конкретное действие (`"paper_throw"`, `"feed_fire"` и т.д.) |
| `fromId`  | string | ID инициатора                                               |
| `toId`    | string | ID цели                                                     |
| `payload` | object | *(опц.)* Данные действия                                    |

---

#### ThemeState (тема `campfire`)

| Поле        | Тип             | Описание                            |
|-------------|-----------------|-------------------------------------|
| `theme`     | string          | `"campfire"`                        |
| `fireLevel` | number          | Уровень огня (0–5)                  |
| `lastFedAt` | string (ISO)    | Время последней подпитки            |
| `trees`     | TreeSnapshot[]  | Деревья на сцене                    |

**TreeSnapshot:**
| Поле        | Тип           | Описание                        |
|-------------|---------------|---------------------------------|
| `id`        | number        | Идентификатор дерева            |
| `x`         | number        | X-позиция на холсте (0–400)     |
| `y`         | number        | Y-позиция на холсте (0–400)     |
| `size`      | number        | Размер дерева                   |
| `burnedAt`  | string (ISO)? | Время сгорания (`null` — живое) |
| `respawnAt` | string (ISO)? | Время возрождения               |

---

### Поток подключения

```
Клиент                                    Сервер
  │                                          │
  ├─── WS connect ──────────────────────────►│
  ├─── subscribe("room:{roomId}") ──────────►│ проверяет существование комнаты
  │◄─── subscribed ──────────────────────────┤
  ├─── rpc("join_room", {...}) ─────────────►│ регистрирует client↔user↔room
  │◄─── JoinRoomResponse ────────────────────┤
  │◄─── publication (RoomSnapshot) ──────────┤ broadcast всем в комнате
  │                    ...                   │
  ├─── rpc("submit_vote", {...}) ───────────►│
  │◄─── {} ──────────────────────────────────┤
  │◄─── publication (RoomSnapshot) ──────────┤ broadcast всем в комнате
```
